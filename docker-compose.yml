
version: '3.8'

services:
  vehicle-paint-app:
    build:
      context: .
      args:
        - API_KEY=${API_KEY}
    ports:
      - "8080:80"
    environment:
      - API_KEY=${API_KEY}
    restart: always
